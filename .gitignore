.env
.env.*
!.env.example
.venv*/
downloads/
*.zip
client_secret*.json
*token*.json
__pycache__/
*.pyc
cache/
output/
inbox/
jobs.db
*.log
logs/
.pytest_cache/
samples/*.mp4
samples/*.wav
assets/music/*.mp3
assets/music/*.part
assets/user_branding/          # user-uploaded logos — never commit
assets/user_fonts/             # user-uploaded fonts (often licensed) — never commit
cache/font_previews/           # generated preview thumbnails
config.local.yaml
tools/                          # local binaries (e.g. driver-compatible ffmpeg) — never commit

# Reference videos for the style analyzer — never commit (may be copyrighted).
refs/

.claude/settings.local.json

.claude/
CLAUDE.md

.env
